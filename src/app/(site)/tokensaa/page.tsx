import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tokens",
};

const TokensaaPage = () => {
  return (
    <div>
      Tokens
    </div>
  )
}

export default TokensaaPage;
