import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sealed",
};
const SealedaaPage = () => {
  return (
    <div>
      Sealed
    </div>
  )
}

export default SealedaaPage;
